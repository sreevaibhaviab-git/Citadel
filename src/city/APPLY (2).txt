CITADEL HAZARD + CASCADE UPGRADE 02
===================================

REPLACE existing files:
  src/city/types.ts
  src/city/sim.ts
  src/city/world.ts
  src/city/CityView.tsx

ADD new file:
  src/city/hud/HazardTimeline.tsx

This patch was built on top of the latest Interaction Polish 01 versions of
world.ts and CityView.tsx, so it preserves the road-hover / selection / search
work from that pass.

AFTER COPYING
-------------
npm run build
npm run dev

QUICK TEST
----------
1. Place FLOOD on map.
   - No single blue flood disk.
   - Water should progressively appear on connected road corridors.
   - T+00 / T+05 / T+15 / T+30 timeline should advance.

2. Place EARTHQUAKE.
   - Assets should show MINOR / MODERATE / SEVERE / FAILED damage labels.
   - Damaged roads should show reduced access.

3. Place FIRE.
   - Fire footprint should grow while fire unit is en route.
   - Growth responds to response ETA / traffic.
   - Footprint should shrink once fire unit is ON SCENE.

4. Trigger GRID CASCADE.
   - T+00 substation failure.
   - T+05 traffic signal degradation.
   - T+15 congestion growth.
   - T+20 ambulance ETA rise.
   - T+30 hospital load increase.
   - Causal lines should appear sequentially instead of all at once.

PROVENANCE
----------
Flood terrain/drainage is a deterministic synthetic proxy for the prototype.
Earthquake vulnerability is modelled.
Cascade relationships are simulated/model-inferred unless backed by live data.
